// examples/demo.ltl — runnable demo for ltl_layout_engine

import std::io
use ltl_layout_engine as lib

fn main() {
    io::println("ltl_layout_engine demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}