// ltl-layout-engine/main.ltl
//
// Flexbox-inspired 2-D layout engine with min/max sizing.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-layout-engine v0.1.0")
    io::println("Flexbox-inspired 2-D layout engine with min/max sizing.")
    0
}
